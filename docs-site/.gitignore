node_modules
.next
.vercel
*.tsbuildinfo
next-env.d.ts

# Build-time copy of the canonical standards library from the engine.
# Refreshed by `prebuild` / `predev` npm scripts; never commit.
lib/standards_library.json
